syntax = "proto3";
package limit.user;

message User {
  string id = 1;
  string pubkey = 2;
}

message GetUserByIdRequest {
  string id = 1;
}

service UserService {
  rpc GetUserById(GetUserByIdRequest) returns (User);
}

